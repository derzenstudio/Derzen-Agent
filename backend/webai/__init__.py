"""
DERZEN - Online AI package.

Kept deliberately empty of imports. The registry is safe to import anywhere,
while the driver pulls in the browser layer, so callers ask for what they need:

    from webai import registry
    from webai import driver
"""
